from math import sqrt
def var():
    vary = 0
    for i in range(2):
        vary+=Py[i]*(y[i] - Ey)**2
        stdy = sqrt(vary)

    return stdx,stdy






def skewness():
    avg=x.mean()
    mod=x.mode()
    vari=x.var()
    std=sqrt(vari)
    skew= (avg-mod)/std
    
    return skew
